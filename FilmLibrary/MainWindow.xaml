<Window x:Class="FilmLibrary.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:l="clr-namespace:FilmLibrary"
         xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Главная страница"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterScreen"
        >
    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="80"></ColumnDefinition>
            <ColumnDefinition Width="500*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.Resources>
            <Style TargetType="materialDesign:PackIcon">
                <Setter Property="Width" Value="20"/>
                <Setter Property="Height" Value="20"/>
            </Style>
        </Grid.Resources>
        <Grid Grid.Column="0">
            <ListView  Margin="0 15">
                <ListViewItem HorizontalAlignment="Center" Margin="0">
                    <Button Style="{StaticResource MaterialDesignFloatingActionButton}" HorizontalAlignment="Left" BorderBrush="{x:Null}" Background="{x:Null}"
                            Foreground="Black"
                            Click="Add_Click"
                            >
                        <StackPanel Margin="-5">
                            <materialDesign:PackIcon Kind="Add">
                            </materialDesign:PackIcon>
                        </StackPanel>
                    </Button>
                </ListViewItem>
                <ListViewItem HorizontalAlignment="Center" Margin="0">
                    <Button Style="{StaticResource MaterialDesignFloatingActionButton}" HorizontalAlignment="Left" BorderBrush="{x:Null}" Background="{x:Null}"
             Foreground="Black"
                            Click="List_Click"
                            >
                        <StackPanel Margin="-5">
                            <materialDesign:PackIcon Kind="ListBoxOutline">
                            </materialDesign:PackIcon>
                        </StackPanel>
                    </Button>
                </ListViewItem>
                <ListViewItem HorizontalAlignment="Center" Margin="0">
                    <Button Style="{StaticResource MaterialDesignFloatingActionButton}" HorizontalAlignment="Left" BorderBrush="{x:Null}" Background="{x:Null}"
             Foreground="Black"
                            Click="Actors_Click"
                            >
                        <StackPanel Margin="-5">
                            <materialDesign:PackIcon Kind="PersonMultiple">
                            </materialDesign:PackIcon>
                        </StackPanel>
                    </Button>
                </ListViewItem>
                <ListViewItem HorizontalAlignment="Center" Margin="0 30 0 0">
                    <Button Style="{StaticResource MaterialDesignFloatingActionButton}" HorizontalAlignment="Left" BorderBrush="{x:Null}" Background="{x:Null}"
Foreground="Black"
                            Click="Exit_Click"
                            >
                        <StackPanel Margin="-5">
                            <materialDesign:PackIcon Kind="ExitToApp" Foreground="Red">
                            </materialDesign:PackIcon>
                        </StackPanel>
                    </Button>
                </ListViewItem>
            </ListView>
        </Grid>
        <Grid Grid.Column="1" Width="auto" x:Name="gridPages" HorizontalAlignment="Stretch" >
            <l:AddPage Loaded="AddPage_Loaded"/>

        </Grid>

    </Grid>

</Window>
